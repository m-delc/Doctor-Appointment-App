import React from "react";

const MakeAppointment = ({ doctorList }) => {
  return <div>null
    
    {/* {doctorList.map(doc => {
      return (
        <li>{doc.name}</li>
        <li>{doc.field}</li>)
      
    })} */}

    
  </div>;
};

export default MakeAppointment;